defaults:
  - _self_
  - launcher/local

hydra:
  output_subdir: null
  mode: MULTIRUN
  run:
    dir: .

env_name: swm/ReacherDMControl-v0
cache_dir: null

world:
  num_envs: 10
  max_episode_steps: 200
  image_shape: [224, 224]

seed: 3072
num_traj: 10000

options: null
